services:
  - type: web
    name: manuel-supermakert
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn BACKEND.pos_api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
